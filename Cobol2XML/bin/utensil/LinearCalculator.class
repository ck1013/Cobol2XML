/**
 * Represents a calculator for linear equations.
 */
public class LinearCalculator {
    
    // instance variables
    /**
     * The starting x-value of the linear equation.
     */
    private double xFrom;

    /**
     * The ending x-value of the linear equation.
     */
    private double xTo;

    /**
     * The starting y-value of the linear equation.
     */
    private double yFrom;

    /**
     * The ending y-value of the linear equation.
     */
    private double yTo;

    // constructor
    /**
     * Constructs a new LinearCalculator object with the given x and y values.
     *
     * @param xFrom The starting x-value of the linear equation.
     * @param xTo   The ending x-value of the linear equation.
     * @param yFrom The starting y-value of the linear equation.
     * @param yTo   The ending y-value of the linear equation.
     */
    public LinearCalculator(double xFrom, double xTo, double yFrom, double yTo) {
        this.xFrom = xFrom;
        this.xTo = xTo;
        this.yFrom = yFrom;
        this.yTo = yTo;
    }

    // method to calculate x for a given y
    /**
     * Calculates the x-value for a given y-value of the linear equation.
     *
     * @param y The y-value for which to calculate the corresponding x-value.
     * @return The x-value that corresponds to the given y-value.
     */
    public double calculateXforGivenY(double y) {
        return (y - yFrom) * (xTo - xFrom) / (yTo - yFrom) + xFrom;
    }

    // method to calculate y for a given x
    /**
     * Calculates the y-value for a given x-value of the linear equation.
     *
     * @param x The x-value for which to calculate the corresponding y-
